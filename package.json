{
  "name": "diagnostico-clubpetro",
  "private": true,
  "version": "5.0.0",
  "type": "module",
  "description": "Diagnóstico de Saúde do Posto · ClubPetro",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
